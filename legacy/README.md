# Legacy — not used for deployment

This is the original single-file version of NerdOutLoud (React + Babel,
loaded via CDN scripts, no build step). It was replaced by the real Vite
production build now living at the repo root.

Kept here only for reference. Netlify does not build anything in this folder.
